<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal (usa colores oscuros por defecto) -->
    <style name="Theme.SymetryCreed" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorSecondary">@color/red_accent</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/background_dark</item>
    </style>

    <!-- Versión sin ActionBar para StartActivity -->
    <style name="Theme.SymetryCreed.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorSecondary">@color/red_accent</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/background_dark</item>
    </style>

</resources>

